import React from 'react';
import LineLeft_ from './../Icons/lineLeft.svg';
const LineLeft = (props) => {
  return (
    <img src={LineLeft_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default LineLeft;
