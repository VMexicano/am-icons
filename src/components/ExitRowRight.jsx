import React from 'react';
import ExitRowRight_ from './../Icons/exitRowRight.svg';
const ExitRowRight = (props) => {
  return (
    <img src={ExitRowRight_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default ExitRowRight;
