import React from 'react';
import ExitRowLeft_ from './../Icons/exitRowLeft.svg';
const ExitRowLeft = (props) => {
  return (
    <img src={ExitRowLeft_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default ExitRowLeft;
