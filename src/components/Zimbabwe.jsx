import React from 'react';
import Zimbabwe_ from './../Icons/zimbabwe.svg';
const Zimbabwe = (props) => {
  return (
    <img src={Zimbabwe_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Zimbabwe;
