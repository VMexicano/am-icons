import React from 'react';
import Moldova_ from './../Icons/moldova.svg';
const Moldova = (props) => {
  return (
    <img src={Moldova_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Moldova;
