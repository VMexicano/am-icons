import React from 'react';
import Checkboxex_ from './../Icons/checkboxex.svg';
const Checkboxex = (props) => {
  return (
    <img src={Checkboxex_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Checkboxex;
