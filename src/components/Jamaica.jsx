import React from 'react';
import Jamaica_ from './../Icons/jamaica.svg';
const Jamaica = (props) => {
  return (
    <img src={Jamaica_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Jamaica;
