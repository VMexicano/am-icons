import React from 'react';
import Grenada_ from './../Icons/grenada.svg';
const Grenada = (props) => {
  return (
    <img src={Grenada_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Grenada;
