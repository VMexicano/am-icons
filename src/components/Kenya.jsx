import React from 'react';
import Kenya_ from './../Icons/kenya.svg';
const Kenya = (props) => {
  return (
    <img src={Kenya_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Kenya;
