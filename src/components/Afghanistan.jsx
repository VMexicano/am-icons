import React from 'react';
import Afghanistan_ from './../Icons/afghanistan.svg';
const Afghanistan = (props) => {
  return (
    <img src={Afghanistan_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Afghanistan;
