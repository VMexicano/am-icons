import React from 'react';
import Slovenia_ from './../Icons/slovenia.svg';
const Slovenia = (props) => {
  return (
    <img src={Slovenia_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Slovenia;
