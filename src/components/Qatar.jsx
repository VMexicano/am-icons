import React from 'react';
import Qatar_ from './../Icons/qatar.svg';
const Qatar = (props) => {
  return (
    <img src={Qatar_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Qatar;
