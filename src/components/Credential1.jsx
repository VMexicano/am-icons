import React from 'react';
import Credential1_ from './../Icons/credential1.svg';
const Credential1 = (props) => {
  return (
    <img src={Credential1_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Credential1;
