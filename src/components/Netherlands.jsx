import React from 'react';
import Netherlands_ from './../Icons/netherlands.svg';
const Netherlands = (props) => {
  return (
    <img src={Netherlands_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Netherlands;
