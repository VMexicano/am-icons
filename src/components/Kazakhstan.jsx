import React from 'react';
import Kazakhstan_ from './../Icons/kazakhstan.svg';
const Kazakhstan = (props) => {
  return (
    <img src={Kazakhstan_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Kazakhstan;
