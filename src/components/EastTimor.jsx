import React from 'react';
import EastTimor_ from './../Icons/eastTimor.svg';
const EastTimor = (props) => {
  return (
    <img src={EastTimor_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default EastTimor;
