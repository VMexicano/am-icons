import React from 'react';
import Libya_ from './../Icons/libya.svg';
const Libya = (props) => {
  return (
    <img src={Libya_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Libya;
