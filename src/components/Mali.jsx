import React from 'react';
import Mali_ from './../Icons/mali.svg';
const Mali = (props) => {
  return (
    <img src={Mali_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Mali;
