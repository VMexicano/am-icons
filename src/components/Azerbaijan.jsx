import React from 'react';
import Azerbaijan_ from './../Icons/azerbaijan.svg';
const Azerbaijan = (props) => {
  return (
    <img src={Azerbaijan_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Azerbaijan;
