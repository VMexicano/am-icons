import React from 'react';
import Albania_ from './../Icons/albania.svg';
const Albania = (props) => {
  return (
    <img src={Albania_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Albania;
