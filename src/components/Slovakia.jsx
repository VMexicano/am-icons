import React from 'react';
import Slovakia_ from './../Icons/slovakia.svg';
const Slovakia = (props) => {
  return (
    <img src={Slovakia_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Slovakia;
