import React from 'react';
import Uganda_ from './../Icons/uganda.svg';
const Uganda = (props) => {
  return (
    <img src={Uganda_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Uganda;
