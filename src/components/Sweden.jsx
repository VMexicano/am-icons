import React from 'react';
import Sweden_ from './../Icons/sweden.svg';
const Sweden = (props) => {
  return (
    <img src={Sweden_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Sweden;
