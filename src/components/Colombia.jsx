import React from 'react';
import Colombia_ from './../Icons/colombia.svg';
const Colombia = (props) => {
  return (
    <img src={Colombia_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Colombia;
