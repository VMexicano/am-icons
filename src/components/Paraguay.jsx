import React from 'react';
import Paraguay_ from './../Icons/paraguay.svg';
const Paraguay = (props) => {
  return (
    <img src={Paraguay_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Paraguay;
