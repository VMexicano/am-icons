import React from 'react';
import Cambodia_ from './../Icons/cambodia.svg';
const Cambodia = (props) => {
  return (
    <img src={Cambodia_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Cambodia;
