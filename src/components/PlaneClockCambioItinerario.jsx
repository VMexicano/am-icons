import React from 'react';
import PlaneClockCambioItinerario_ from './../Icons/planeClockCambioItinerario.svg';
const PlaneClockCambioItinerario = (props) => {
  return (
    <img src={PlaneClockCambioItinerario_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default PlaneClockCambioItinerario;
