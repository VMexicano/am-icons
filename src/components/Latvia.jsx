import React from 'react';
import Latvia_ from './../Icons/latvia.svg';
const Latvia = (props) => {
  return (
    <img src={Latvia_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Latvia;
