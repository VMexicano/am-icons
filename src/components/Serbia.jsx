import React from 'react';
import Serbia_ from './../Icons/serbia.svg';
const Serbia = (props) => {
  return (
    <img src={Serbia_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Serbia;
