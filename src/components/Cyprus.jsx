import React from 'react';
import Cyprus_ from './../Icons/cyprus.svg';
const Cyprus = (props) => {
  return (
    <img src={Cyprus_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Cyprus;
