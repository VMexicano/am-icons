import React from 'react';
import Iraq_ from './../Icons/iraq.svg';
const Iraq = (props) => {
  return (
    <img src={Iraq_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Iraq;
