import React from 'react';
import Tunisia_ from './../Icons/tunisia.svg';
const Tunisia = (props) => {
  return (
    <img src={Tunisia_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Tunisia;
