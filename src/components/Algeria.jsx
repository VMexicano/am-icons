import React from 'react';
import Algeria_ from './../Icons/algeria.svg';
const Algeria = (props) => {
  return (
    <img src={Algeria_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Algeria;
