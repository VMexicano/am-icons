import React from 'react';
import Laos_ from './../Icons/laos.svg';
const Laos = (props) => {
  return (
    <img src={Laos_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Laos;
