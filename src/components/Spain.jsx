import React from 'react';
import Spain_ from './../Icons/spain.svg';
const Spain = (props) => {
  return (
    <img src={Spain_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Spain;
