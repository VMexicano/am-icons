import React from 'react';
import Guatemala_ from './../Icons/guatemala.svg';
const Guatemala = (props) => {
  return (
    <img src={Guatemala_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Guatemala;
