import React from 'react';
import TabletIpad_ from './../Icons/tabletIpad.svg';
const TabletIpad = (props) => {
  return (
    <img src={TabletIpad_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default TabletIpad;
