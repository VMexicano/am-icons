import React from 'react';
import SeatsIconPremierLight_ from './../Icons/seatsIconPremierLight.svg';
const SeatsIconPremierLight = (props) => {
  return (
    <img src={SeatsIconPremierLight_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default SeatsIconPremierLight;
