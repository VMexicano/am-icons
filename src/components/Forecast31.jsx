import React from 'react';
import Forecast31_ from './../Icons/forecast31.svg';
const Forecast31 = (props) => {
  return (
    <img src={Forecast31_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Forecast31;
