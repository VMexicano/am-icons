import React from 'react';
import Myanmar_ from './../Icons/myanmar.svg';
const Myanmar = (props) => {
  return (
    <img src={Myanmar_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Myanmar;
