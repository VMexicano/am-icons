import React from 'react';
import Bulgaria_ from './../Icons/bulgaria.svg';
const Bulgaria = (props) => {
  return (
    <img src={Bulgaria_} alt={props.alt || ''} {...props} >
    </img>
  );
};
export default Bulgaria;
